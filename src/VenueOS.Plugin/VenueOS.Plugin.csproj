<Project Sdk="Dalamud.NET.Sdk/15.0.0">
  <PropertyGroup><TargetFramework>net10.0-windows</TargetFramework><AssemblyName>VenueOS</AssemblyName><RootNamespace>VenueOS.Plugin</RootNamespace><AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath><Version>0.1.0</Version><AssemblyVersion>0.1.0.0</AssemblyVersion><FileVersion>0.1.0.0</FileVersion><Name>VenueOS</Name><Author>VenueOS</Author><Punchline>Multi-venue operations console.</Punchline><Description>Venue-scoped operations, promotion, and backend-compatible venue games.</Description><ApplicableVersion>any</ApplicableVersion><AllowUnsafeBlocks>true</AllowUnsafeBlocks></PropertyGroup>
  <ItemGroup><ProjectReference Include="..\VenueOS.Core\VenueOS.Core.csproj" /><ProjectReference Include="..\VenueOS.Venues\VenueOS.Venues.csproj" /><ProjectReference Include="..\VenueOS.Services\VenueOS.Services.csproj" /><ProjectReference Include="..\VenueOS.UI\VenueOS.UI.csproj" /><ProjectReference Include="..\VenueOS.Modules.Operations\VenueOS.Modules.Operations.csproj" /><Reference Include="Dalamud.Bindings.ImGui" Private="false" /><Reference Include="FFXIVClientStructs" Private="false" />
    <!-- Party Finder reconstruction: VenueOS owns its automation engine directly (src/VenueOS.Plugin/PartyFinder/)
         and depends on ECommons the same way the read-only donor reference (venuepartyfinder) does — a third-party
         library reference, not a live ProjectReference to the donor's own source. See PARTY_FINDER_RECONSTRUCTION.md.
         Pulled as a real PackageReference (not a hard-coded HintPath into one machine's NuGet cache) and pinned to
         a version built against Dalamud API level 15 — see ECOMMONS_UNLOAD_FIX.md for why 3.1.0.19 broke unload. -->
    <PackageReference Include="ECommons" Version="3.2.1.18" />
  </ItemGroup>
  <ItemGroup><None Update="VenueOS.json"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></None></ItemGroup>
</Project>
